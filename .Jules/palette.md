# Palette's Journal - Critical Learnings

## 2025-02-23 - Icon-Only Buttons in Modals

**Learning:** Icon-only buttons without `aria-label` or tooltips create accessibility barriers for screen reader users and touch/mouse navigation.
**Action:** Always provide explicit `aria-label` and `title` or dynamic tooltips for icon-only action buttons in modals and lists.
